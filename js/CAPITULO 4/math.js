let numero

numero = Math.sqrt(25) // raiz cuadrada
numero = Math.cbrt(27) // raiz cubica

numero = Math.max(4, 1,2,234424, 34, 12,21,32) // me devuelve el numero mas grande de todo
numero = Math.min(4, 1,2,234424, 34, 12,21,32) // me devuelve el numero mas pequeno de todo
numero = Math.random()*100 // numero random
// document.write(numero + "<br>")
numero = Math.floor(numero) // devuelve el piso de un numero
numero = Math.round(numero) // devuelve un numero redondeado
numero = Math.fround(9.2344423494834578934478945893749842) // devuelve la representacion flotante de precicion simple mas cercaana de un numero
numero = Math.trunc(numero) // si hay decimales los elimina todos
numero = Math.PI // me da el valor de pi
numero = Math.SQRT1_2 // valor de raiz cuadrada de 1/2 
numero = Math.SQRT2 // valor de raiz cuadrada 2
numero = Math.E // valor de Euler
numero = Math.LN2 // logaritmo natural de 2
numero = Math.LN10 // logaritmo natural de 10
numero = Math.LOG2E  // logaritmo de E con base 2
numero = Math.LOG10E  // logaritmo de E con base 10

document.write(numero)
